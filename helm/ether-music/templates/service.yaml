apiVersion: v1
kind: Service
metadata:
  name: {{ include "ether-music.fullname" . }}
  labels:
    {{- include "ether-music.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ether-music.selectorLabels" . | nindent 4 }}
